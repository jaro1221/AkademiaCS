<Window x:Class="Hurtownia.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hurtownia.Windows"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <ListView Name="ListViewSettings" Grid.Row="0" MouseDoubleClick="ListViewSettings_OnMouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Option" Width="auto" DisplayMemberBinding="{Binding Key}" />
                    <GridViewColumn Header="Value" Width="auto" DisplayMemberBinding="{Binding Value}" />
                </GridView>

            </ListView.View>
        </ListView>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1">
            <Button Name="ButtonSave" Width="50" Content="Zapisz" Click="ButtonSave_Click" Height="25"
                    Margin="0,0,0,-0.2" VerticalAlignment="Top" />
            <Button Name="ButtonCancel" Width="50" Content="Anuluj" Margin="5,0,0,0" Click="ButtonCancel_Click" />
        </StackPanel>
    </Grid>
</Window>